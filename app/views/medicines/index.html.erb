<div class="container-fluid">
  <div class="container">
    <div class="row">

      <div class="title">
        <h1>medicine#index</h1>
      </div>
    </div>

      <%= link_to "Add medicines", new_medicine_path, class: "btn btn-info btn-md"%>

        <%#= render partial: 'form', locals: { medicine: @medicine } %>

        <div class="medicines_area">
          <%= render partial: 'index', locals: { medicines: @medicines } %>
        </div>

  </div>
</div>
