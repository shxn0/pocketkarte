  <%# indexアクションで@medicinesのインスタンス変数を生成しているため、nilのインスタンスがページ読み込みと同時に作成されてしまう。%>
  <%# そのnilインスタンスをviewに表示させないために、unlessの条件式で表示するインスタンスを制御している %>


  <% medicines.each do |medicine| %>
    <% unless medicine.id.nil? %>

    <div class="row col-md-3">
      <div class="panel panel-default">
        <div class="panel-body">
          内服薬の画像
        </div>

        <!-- <ul class="list-group">
          <li class="list-group-item">Start Date</li>
        </ul> -->

        <div class="panel-footer">
          <%= medicine[:medicine_name] %>
          <span class="text-right">
            <%= link_to "", medicine_path(medicine), method: :delete, class:"now-ui-icons ui-1_simple-remove" %></span>
          </span>
      	</div>
      </div>


    </div>


    <% end %>
  <% end %>
